import React, { Component } from "react";
import ReactDOM from "react-dom";

const header = () => (
    <div className="header">
        <h1 className="header__title">Daly 5th Grade</h1>
    </div>
);

export default header;